--Ваши замечания и работа над ними:

1. address TEXT - текста многовато, должно хватить строки.
-> устранено. Поставил VARCHAR(255)

2. В orders предусмотрена только одна экскурсия, может быть несколько.
-> В теории можно сделать еще одну таблицу отношений между экскурсиями и
клиентами, но я решил просто добавить еще два столбца под экскурсии:
	`excursions_id_1` INT UNSIGNED NOT NULL,
	`excursions_id_2` INT UNSIGNED NOT NULL,
	`excursions_id_3` INT UNSIGNED NOT NULL,
Соответственно подкорректировал остальные файлы: с вносимыми данными, ключами. Учёл новые два столбца.
Возникла проблема с тем, что вносить в столбец с экскурсиями, если клиент не захотел брать их. 
NULL не поставишь, т.к. много где есть ограничение NOT NULL. 
Решил сделать так: добавил в систему отель с id=13, через внешний ключ связал его с 
таблицей экскурсий id=2 (id=1 почему-то не хочет создаваться).
В случае отказа от экскурсий, в поле заказа будет вставляться цифра 2 .

insert into 
	`hotels` (name, countries_code_id, regions_id, price, stars) 
values
	('TEZTOUR', 'TUR', '1', '1','five' );

insert into 
	`excursions` (hotels_id , name , description , price )
values ( '13', ('Отказ от экскурсии'), (NULL), (NULL) ); 

Не очень красиво, согласен, но работает. Уж лучше бы сделал отдельно таблицу. 
Но это опыт. Метод проб и ошибок...Теперь знаю, что так неудобно.

3. В триггере 'UPDATE to NULL is not permitted' - желательно указать о каком значении говорится.
-> исправил SET MESSAGE_TEXT = 'UPDATE to NULL field "passport" is not permitted';

4. Буквально в 2-х словах написал о чем моя БД

5. Проанализировав БД, я пришел к выводу, что она не особо нуждается в создании каких-либо индексов.
Единственное, я бы мог создать индекс для таблицы `services` для столбца `name`.
CREATE INDEX services_name_idx ON services(name);
